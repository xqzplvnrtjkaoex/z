---
phase: quick
plan: 260325-1c6
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/rules/rust-documentation.md
  - .planning/phases/03a-authentication-core/03a-CONTEXT.md
  - .planning/STATE.md
autonomous: true
must_haves:
  truths:
    - "No documentation references deleted domain/input/ module or HandleInput/NameInput types"
    - "03a-CONTEXT.md Established Patterns section reflects payload/ module instead of domain/input/"
    - "STATE.md decisions are consistent with current codebase state"
  artifacts:
    - path: ".claude/rules/rust-documentation.md"
      provides: "Updated per-layer rustdoc table reflecting payload module"
    - path: ".planning/phases/03a-authentication-core/03a-CONTEXT.md"
      provides: "Accurate codebase reference patterns for auth phase planning"
  key_links: []
---

<objective>
Update documentation files that reference the now-deleted `domain/input/` module (HandleInput, NameInput, HandleValidationError) and ensure they reflect the new `payload/` module with validator-based validation pattern.

Purpose: The user service was refactored to move validation from `domain/input/` structs to `payload/` structs with `#[derive(Validate)]`. Several docs still reference the old structure.
Output: Updated docs with accurate references
</objective>

<context>
@CLAUDE.md
@.claude/rules/rust-documentation.md
@.planning/phases/03a-authentication-core/03a-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update rust-documentation.md stale HandleInput/domain-input references</name>
  <files>.claude/rules/rust-documentation.md</files>
  <action>
The per-layer rustdoc table at line 59 references `domain input` layer with `HandleInput` example. This layer no longer exists -- validation moved to `payload/` module.

Update the table:
- Change row `| **domain input** (`HandleInput`) | YES | State validation rules (constraints are business logic) |`
  to `| **payload** (`CreateUserPayload`) | Selective | Only when validation rules or normalization logic is non-obvious |`
- Update the summary line example at line 74-75: replace `HandleInput` with a payload example. Change:
  ```
  /// Validates a user handle: 4-15 alphanumeric/underscore chars, not reserved.
  pub struct HandleInput { ... }
  ```
  to:
  ```
  /// Lists users with cursor-based pagination and optional inactive filter.
  pub struct ListUsersPayload { ... }
  ```
- Update SSOT intra-doc links at line 107: replace `[`HandleInput`]` with `[`CreateUserPayload`]`
  </action>
  <verify>grep -n "HandleInput\|NameInput\|domain input" .claude/rules/rust-documentation.md | wc -l should be 0</verify>
  <done>No references to HandleInput, NameInput, or domain input layer remain in rust-documentation.md</done>
</task>

<task type="auto">
  <name>Task 2: Update 03a-CONTEXT.md established patterns section</name>
  <files>.planning/phases/03a-authentication-core/03a-CONTEXT.md</files>
  <action>
Lines 259 and 262 reference the old structure. This is critical because plan-phase will read this file to understand codebase patterns when planning auth.

Line 259 — change:
  `- **4-layer architecture:** domain/ (types/, ports/, error/, input/) -> usecase/ -> app/rpc/ -> adapter/`
to:
  `- **4-layer architecture:** domain/ (types/, ports/, error/) -> usecase/ -> app/rpc/ -> adapter/ + top-level payload/ module`

Line 262 — change:
  `- **Domain input validation:** `HandleInput`, `NameInput` structs in `domain/input/` with validation methods and `HandleValidationError` enum`
to:
  `- **Payload validation:** `#[derive(Validate)]` structs in `payload/` module with getter methods for normalization. `From<ValidationErrors>` converts to `InvalidInput` error variant. See `.claude/rules/rust-conventions.md` Input Validation section.`
  </action>
  <verify>grep -n "HandleInput\|NameInput\|domain/input\|HandleValidationError" .planning/phases/03a-authentication-core/03a-CONTEXT.md | wc -l should be 0</verify>
  <done>03a-CONTEXT.md Established Patterns accurately describes payload/ module pattern for auth phase planning</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md stale decision references</name>
  <files>.planning/STATE.md</files>
  <action>
Line 81 references the old pattern. Update:

Change:
  `- [Phase 02-user-profile]: Handle validation uses custom check_handle_chars function (not #[validate(regex)] attribute); LazyLock<Regex> replaced by bytes().all() in quick task 260324-rfl -- regex crate fully removed`
to:
  `- [Phase 02-user-profile]: Handle validation uses #[derive(Validate)] on payload structs with custom validator fns (check_handle_chars, check_reserved_handle) in payload/ module; regex crate fully removed`

Also check line 83 — references classify_db_err which was replaced by `From<DbErr>` impl. That reference already says "(now replaced by From<DbErr> impl per quick task 260322-vwa)" so it is accurate as historical context. Leave it.

No changes needed to CLAUDE.md itself — the Workspace Structure section at line 170 says `domain/ (types, ports, errors)` which is already correct (never listed `input/`). The CLAUDE.md diff is just the Developer Profile addition which is fine.
  </action>
  <verify>grep -n "LazyLock\|domain/input" .planning/STATE.md | wc -l should be 0</verify>
  <done>STATE.md decisions reflect current validation pattern</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep -rn "HandleInput\|NameInput\|HandleValidationError\|domain/input\|domain.*input/" .claude/rules/ CLAUDE.md .planning/STATE.md .planning/phases/03a-authentication-core/03a-CONTEXT.md` returns no matches
2. `grep -n "payload/" .claude/rules/rust-documentation.md .planning/phases/03a-authentication-core/03a-CONTEXT.md` returns matches confirming new references exist
</verification>

<success_criteria>
- Zero references to deleted domain/input/ module, HandleInput, NameInput, or HandleValidationError in updated docs
- 03a-CONTEXT.md accurately describes payload/ pattern for upcoming plan-phase consumption
- STATE.md decisions reflect current validation approach
- CLAUDE.md confirmed accurate (no changes needed beyond existing Developer Profile diff)
</success_criteria>

<output>
After completion, create `.planning/quick/260325-1c6-check-and-update-docs-for-current-change/260325-1c6-SUMMARY.md`
</output>
