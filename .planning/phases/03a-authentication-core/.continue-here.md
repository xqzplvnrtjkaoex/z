---
phase: 03a-authentication-core
task: discuss-phase (context update)
total_tasks: 6
status: in_progress
last_updated: 2026-03-24T14:55:04.136Z
---

<current_state>
Running `/gsd:discuss-phase 3a` to UPDATE existing CONTEXT.md. 4 gray areas selected for discussion, 2 completed. Advisor mode active — all 4 advisor research agents already ran and returned structured comparison tables.
</current_state>

<completed_work>

- Area 1: JWT middleware transition - Done
  - Replace `extract_caller_identity` with `verify_jwt` via `route_layer` + `from_fn_with_state`
  - CallerIdentity extended: add `handle: String`, `session_id: Uuid`
  - Public vs protected routes structurally split
  - Grace period handled via response mutation (Set-Cookie after next.run())

- Area 2: Dev environment setup - Done
  - JWT keys: `just dev-keys` recipe (openssl ES256 PEM → `.env`, gitignored)
  - Docker-compose: auth-db (postgres:18-alpine, 5434) + redis (redis:8-alpine, 6379)
  - Seed invite: fixed dev token (SHA-256 hash in migration). **Prod seed MUST be separate**
</completed_work>

<remaining_work>

- Area 3: Cross-service error handling - NOT STARTED
  - Auth → User gRPC calls (CreateUser on registration, GetUser on login)
  - Advisor recommends: fail-fast + call ordering (User service FIRST, then Auth DB) + Endpoint timeout
  - User understood the topic but hasn't chosen yet

- Area 4: Test orchestration - NOT STARTED
  - madome-tests structure, container sharing, SoftPasskey integration
  - Advisor recommends: per-binary OnceCell + in-process service harness for E2E
  - Not yet presented to user

- Area 5: Phase 2 code refactoring reflection
  - User noted Phase 2 docs are outdated, code heavily refactored
  - code_context section of CONTEXT.md needs updating with actual patterns

- Area 6: Write updated CONTEXT.md + DISCUSSION-LOG.md + commit
</remaining_work>

<decisions_made>

- verify_jwt middleware replaces extract_caller_identity entirely — JWT is sole identity source, route_layer prevents 404→401 bleed
- CallerIdentity extended with handle + session_id — D-39 JWT claims, D-49 gRPC metadata propagation
- just dev-keys recipe — zero committed secrets, aligns with justfile convention
- Docker-compose adds auth-db + redis — extends existing user-db pattern
- Fixed dev seed token — deterministic for automation. User strongly emphasized prod separation
</decisions_made>

<blockers>
None
</blockers>

<context>
The existing CONTEXT.md already has ~60 decisions from the Phase 3 split (2026-03-25). This discuss-phase session is an UPDATE to add new decisions about middleware architecture, dev environment, error handling, and testing. Advisor mode spawned 4 parallel research agents (sonnet model) — all completed and returned comparison tables with rationale. The uncommitted files in git are from the Phase 3→3A/3B split (old 03-authentication/ directory deletions + ROADMAP/CLAUDE.md updates).
</context>

<next_action>
Start with: `/gsd:discuss-phase 3a` → "Update it" → present cross-service error handling options (advisor research already done, show table) → then test orchestration → update code_context → write CONTEXT.md
</next_action>
